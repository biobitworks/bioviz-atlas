# bioviz-atlas Dashboard

The hackathon exemplar's "dashboard" is the static `index.html`
at the repo root, plus the operator surfaces under:

- [`output/`](../output/) — generated output.
- [`experiments/`](../experiments/) — experiment notebooks.
- [`.planning/`](../.planning/) — plans.

Resolves `dashboard_missing`.
