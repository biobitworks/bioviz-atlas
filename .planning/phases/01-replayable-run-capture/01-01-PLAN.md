---
phase: "01-replayable-run-capture"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, src/lib/capture.ts]
autonomous: true
requirements: [DATA-01, DATA-05]
user_setup: []
must_haves:
  truths:
    - "The repo ignores `.bioviz-runs/`, and capture helpers can create deterministic hashes, canonical JSON, stable run paths, atomic JSON files, and append-only JSONL rows."
  artifacts:
    - "src/lib/capture.ts exports the capture helpers used by later server integration work."
  key_links:
    - "`.gitignore` ignores `.bioviz-runs/` so runtime artifacts stay out of source control."
    - "`src/lib/capture.ts` exposes helpers that later plans can import from `server.ts` and `src/lib/run-manifest.ts`."
---

<objective>
Create the capture foundation for Phase 1.

Purpose: Phase 1 needs deterministic filesystem helpers before the existing analysis route can write replayable artifacts.
Output: A gitignored `.bioviz-runs/` runtime root and a `src/lib/capture.ts` module with hashing, canonical JSON, path, atomic write, and JSONL append helpers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-replayable-run-capture/01-RESEARCH.md
@.planning/phases/01-replayable-run-capture/01-VALIDATION.md
@server.ts
@src/lib/analysis.ts
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the runtime ignore and create the capture helper module</name>
  <files>.gitignore, src/lib/capture.ts</files>
  <read_first>.gitignore, server.ts, src/lib/analysis.ts, .planning/phases/01-replayable-run-capture/01-RESEARCH.md</read_first>
  <action>Add `.bioviz-runs/` to `.gitignore`. Create `src/lib/capture.ts` and export `sha256`, `canonicalJson`, `canonicalJsonSha256`, `slugifyModelName`, `createRunPaths`, `writeJsonAtomic`, and `appendJsonl`. Keep the module dependency-light by using Node built-ins already present in the repo. `createRunPaths` must build run folders under `.bioviz-runs/runs/YYYY/MM/DD/{runId}` and index paths under `.bioviz-runs/index/`.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `.gitignore contains ".bioviz-runs/"`
    - `src/lib/capture.ts contains "export function sha256("`
    - `src/lib/capture.ts contains "export function canonicalJson("`
    - `src/lib/capture.ts contains "export function createRunPaths("`
    - `src/lib/capture.ts contains "export async function writeJsonAtomic("`
    - `src/lib/capture.ts contains "export async function appendJsonl("`
  </acceptance_criteria>
  <done>The repo ignores runtime artifacts and the helper module exists with the required exports.</done>
</task>

<task type="auto">
  <name>Task 2: Make the capture helpers deterministic and replay-safe</name>
  <files>src/lib/capture.ts</files>
  <read_first>src/lib/capture.ts, src/lib/analysis.ts, .planning/research/DATA_CAPTURE.md</read_first>
  <action>Implement `canonicalJson` so object keys are sorted recursively while array order is preserved. Make `canonicalJsonSha256` hash the canonical JSON string. Implement `writeJsonAtomic` by writing a temporary sibling file and renaming it into place. Implement `appendJsonl` so each row is canonical JSON followed by a single newline and parent directories are created automatically. Keep helper output ASCII-safe and stable across runs.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/lib/capture.ts contains "Object.keys(value).sort()"`
    - `src/lib/capture.ts contains "await fs.mkdir(path.dirname(filePath), { recursive: true })"`
    - `src/lib/capture.ts contains "await fs.rename("`
    - `src/lib/capture.ts contains "await fs.appendFile("`
  </acceptance_criteria>
  <done>The capture helpers are deterministic, create parent directories, and write files atomically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsx --eval "import('./src/lib/capture.ts').then((m) => console.log(typeof m.canonicalJson, typeof m.createRunPaths, typeof m.writeJsonAtomic))"` prints `function function function`
</verification>

<success_criteria>

- All tasks completed
- Capture helpers exist and compile
- `.bioviz-runs/` is ignored by git
- Helper APIs support deterministic hashing and file-safe persistence
  </success_criteria>

<output>
After completion, create `.planning/phases/01-replayable-run-capture/01-01-SUMMARY.md`
</output>
