---
phase: "01-replayable-run-capture"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [server.ts]
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]
user_setup: []
must_haves:
  truths:
    - "The `/api/analyze` handler preserves prompt text, provider attempt metadata, raw provider responses, parsed model output, fallback output, final output, and errors as explicit stage data before capture writes happen."
    - "The route still responds with the existing `BioVizReport` payload shape."
  artifacts:
    - "server.ts keeps a single request orchestration path that exposes capture-ready stage variables."
  key_links:
    - "`server.ts` records provider/model/options and validation outcomes in the same request that returns the final report."
    - "`server.ts` preserves the final `BioVizReport` response boundary while exposing internal stage data for later manifest writing."
---

<objective>
Refactor the existing analysis route so replay-critical stage data survives until capture time.

Purpose: The app already computes most of the fields Phase 1 needs, but it currently discards prompt text, raw model responses, validation failures, and provider attempt metadata.
Output: A capture-ready `/api/analyze` flow with explicit stage variables and no change to the response contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-replayable-run-capture/01-RESEARCH.md
@.planning/phases/01-replayable-run-capture/01-VALIDATION.md
@server.ts
@src/lib/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make request-stage outputs explicit in the analysis handler</name>
  <files>server.ts</files>
  <read_first>server.ts, src/lib/schema.ts, .planning/phases/01-replayable-run-capture/01-RESEARCH.md</read_first>
  <action>Refactor `app.post("/api/analyze", ...)` so the handler stores replay-critical data in explicit local variables or typed records instead of letting it disappear inside provider branches. Preserve named values for `renderedPrompt`, `providerAttempts`, `rawModelResponse`, `parsedModelReport`, `fallbackReport`, `finalReport`, and `validationError`. Keep fallback construction and final hydration in one place so the future capture step can serialize each stage without re-running business logic.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `server.ts contains "const providerAttempts"`
    - `server.ts contains "renderedPrompt"`
    - `server.ts contains "fallbackReport"`
    - `server.ts contains "finalReport"`
    - `server.ts contains "validationError"`
  </acceptance_criteria>
  <done>The route exposes stable stage variables for later manifest capture.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve provider metadata and failures without changing the API response</name>
  <files>server.ts</files>
  <read_first>server.ts, src/lib/schema.ts, .planning/research/METHODOLOGY.md</read_first>
  <action>Update the provider execution path so each provider attempt records provider name, model name, parameters used, attempt order, raw text when available, schema parse status, and any thrown error message. Ensure the handler still ends with `res.json(finalReport)` and does not add new response fields in Phase 1. Keep fallback-only and schema-invalid cases represented in internal stage data so Plan 03 can write them to disk.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `server.ts contains "attemptOrder"`
    - `server.ts contains "rawModelResponse"`
    - `server.ts contains "res.json(finalReport)"`
    - `server.ts contains "provider:"`
    - `server.ts contains "model:"`
  </acceptance_criteria>
  <done>The handler keeps provider metadata and failures while still returning the existing report contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `rg -n "renderedPrompt|providerAttempts|fallbackReport|finalReport|validationError" server.ts`
</verification>

<success_criteria>

- All tasks completed
- `server.ts` preserves replay-critical stage data
- Provider/model metadata and failures are represented explicitly
- The `/api/analyze` response remains the existing `BioVizReport`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-replayable-run-capture/01-02-SUMMARY.md`
</output>
