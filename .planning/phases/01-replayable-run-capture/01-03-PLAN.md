---
phase: "01-replayable-run-capture"
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [server.ts, src/lib/capture.ts, src/lib/run-manifest.ts]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
user_setup: []
must_haves:
  truths:
    - "Every completed, fallback-only, or failed single-run analysis writes a manifest with input hashes, provider/model metadata, status, validation outcome, and links to replay-critical artifact files."
    - "Each run stores raw response text, parsed model JSON when available, fallback report, final report, and provenance data in `.bioviz-runs/` without changing the browser-visible report."
    - "Replay-critical fields are validated before the run index is appended."
  artifacts:
    - "src/lib/run-manifest.ts defines the manifest schema or validator and replay-critical requirements."
    - "`.bioviz-runs/index/runs.jsonl` is written through the capture helpers as an append-only summary index."
  key_links:
    - "`server.ts` calls the capture helpers after the final report is built and before the request returns."
    - "`src/lib/run-manifest.ts` validates the manifest object that references files written by `src/lib/capture.ts`."
---

<objective>
Persist one replayable run bundle and validate it before indexing.

Purpose: Phase 1 is only complete when the existing single-run route writes inspectable artifacts for both successful and degraded runs.
Output: A validated run manifest, replay-critical sidecar files, and an append-only JSONL index under `.bioviz-runs/`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-replayable-run-capture/01-RESEARCH.md
@.planning/phases/01-replayable-run-capture/01-VALIDATION.md
@server.ts
@src/lib/capture.ts
@src/lib/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 1 run manifest contract and replay-critical validator</name>
  <files>src/lib/run-manifest.ts</files>
  <read_first>src/lib/schema.ts, .planning/research/DATA_CAPTURE.md, .planning/phases/01-replayable-run-capture/01-RESEARCH.md</read_first>
  <action>Create `src/lib/run-manifest.ts` with a typed manifest contract for Phase 1. Include fields for `runId`, timestamps, `status`, input hashes, provider/model metadata, prompt identity, provenance summary, validation state, and file paths for `prompt.txt`, `response.raw.txt`, `response.parsed.json`, `fallback.report.json`, and `report.json`. Export a validator or Zod schema plus a helper that checks required replay-critical fields before the index row is written.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `src/lib/run-manifest.ts contains "runId"`
    - `src/lib/run-manifest.ts contains "response.raw.txt"`
    - `src/lib/run-manifest.ts contains "fallback.report.json"`
    - `src/lib/run-manifest.ts contains "report.json"`
    - `src/lib/run-manifest.ts contains "export"`
  </acceptance_criteria>
  <done>A manifest schema or validator exists for the replay-critical Phase 1 run bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Write the run bundle and append the run index from the existing route</name>
  <files>server.ts, src/lib/capture.ts, src/lib/run-manifest.ts</files>
  <read_first>server.ts, src/lib/capture.ts, src/lib/run-manifest.ts, .planning/research/DATA_CAPTURE.md</read_first>
  <action>Integrate the capture flow into `server.ts` after `finalReport` is ready. Use `createRunPaths`, `writeJsonAtomic`, and `appendJsonl` to write the Phase 1 bundle: `manifest.json`, `prompt.txt`, `response.raw.txt` when provider text exists, `response.parsed.json` when parsed JSON exists, `fallback.report.json`, `report.json`, and any validation-error payload. Compute hashes for sequence, question, organism, optional PDF, prompt, fallback report, final report, and raw response where present. Write `.bioviz-runs/index/runs.jsonl` only after manifest validation passes.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `server.ts contains "manifest.json"`
    - `server.ts contains "prompt.txt"`
    - `server.ts contains "response.raw.txt"`
    - `server.ts contains "response.parsed.json"`
    - `server.ts contains "fallback.report.json"`
    - `server.ts contains "report.json"`
    - `server.ts contains "runs.jsonl"`
  </acceptance_criteria>
  <done>The route writes a validated run bundle and appends a JSONL summary row.</done>
</task>

<task type="auto">
  <name>Task 3: Prove non-regression and replay-critical coverage with a local smoke path</name>
  <files>server.ts, src/lib/run-manifest.ts</files>
  <read_first>server.ts, src/lib/run-manifest.ts, src/lib/schema.ts, .planning/phases/01-replayable-run-capture/01-VALIDATION.md</read_first>
  <action>Add the minimum local verification support needed to prove Phase 1 works without UI changes. Ensure a single `/api/analyze` request can still return a valid `BioVizReport` while the manifest validator confirms required replay-critical fields. Keep the implementation local to the current stack; do not add new frameworks or Phase 2 comparison behavior.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `server.ts contains "BioVizReportSchema"`
    - `src/lib/run-manifest.ts contains "status"`
    - `src/lib/run-manifest.ts contains "provider"`
    - `src/lib/run-manifest.ts contains "validation"`
  </acceptance_criteria>
  <done>The Phase 1 implementation preserves the existing report contract while validating replay-critical run data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsx --eval "import('./src/lib/run-manifest.ts').then((m) => console.log(Object.keys(m).length > 0 ? 'ok' : 'missing'))"` prints `ok`
- [ ] Run one local `/api/analyze` request and confirm a new `.bioviz-runs/runs/YYYY/MM/DD/.../manifest.json` exists plus `.bioviz-runs/index/runs.jsonl` gains one row
</verification>

<success_criteria>

- All tasks completed
- Every single-run analysis writes a replayable bundle under `.bioviz-runs/`
- Replay-critical fields are validated before indexing
- The browser-visible report contract remains unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/01-replayable-run-capture/01-03-SUMMARY.md`
</output>
