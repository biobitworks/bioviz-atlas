---
phase: 00-ad-hoc-bioviz-atlas
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-04-13T07:45:07.900Z
---

<current_state>
BioViz Atlas is paused after backend stabilization. The local prototype now prefers OpenAI `gpt-4.1-mini` for fast synthesis, with Gemini and local Ollama kept as fallback paths. The canonical identity bug for plain FASTA headers is fixed, and the GLP-1 peptide demo input now resolves to public-safe identifiers instead of showing empty canonical data. The app server is currently running on `http://localhost:3000`.
</current_state>

<completed_work>

- Task 1: Fix canonical identity fallback for plain FASTA headers - Done
  - `>GLP-1 (7-37) Human` now resolves to `GCG / P01275`
  - public links and gene identifiers are filled from UniProt-derived data
- Task 2: Add OpenAI fast synthesis path and strict JSON schema support - Done
  - `server.ts` now calls OpenAI Responses first when `OPENAI_API_KEY` is present
  - `schema.ts` now exports a concrete strict JSON schema compatible with OpenAI structured output
  - deterministic fields are hydrated back into the report after model synthesis
- Task 3: Verify GLP-1 peptide and GLP1R receptor analysis end to end - In progress
  - API path verified for the GLP-1 peptide input
  - API path verified for the user-provided `GLP1R_HUMAN` FASTA plus the uploaded GLP-1-related paper
  - `npm run lint` passes
</completed_work>

<remaining_work>

- Task 3: Finish the browser-side verification that the UI renders the same corrected payload from `localhost:3000`
- Task 4: Capture a fresh screenshot or recording artifact under `output/playwright/` using the corrected flow
- Task 5: Rerecord or update the 1-minute demo so it reflects the GPT-backed path and the fixed canonical/provenance behavior
</remaining_work>

<decisions_made>

- Decided to use OpenAI `gpt-4.1-mini` as the primary fast model path because `OPENAI_API_KEY` was already present and the user asked for a GPT path closer to Gemini Flash behavior.
- Kept deterministic hydration of canonical identity, links, paper mentions, and provenance after model synthesis because model-only output was dropping key demo fields.
- Added a public-safe known-protein fallback for the GLP-1 peptide path because the plain FASTA header lacked a UniProt accession and was producing empty canonical data.
- Avoided inventing new biological claims; all descriptions remain exploratory and non-clinical.
</decisions_made>

<blockers>
- Browser automation is not fully closed out yet: Playwright CLI is available, but the repo-local module resolution path used during scripted checks did not expose the `playwright` package for ad hoc Node execution.
- No fresh browser screenshot/video was produced after the latest backend fixes.
</blockers>

<context>
The backend is in substantially better shape than the UI recording status suggests. The real unresolved issue is not protein resolution or report synthesis anymore; it is the last mile of browser automation and demo asset regeneration. The most efficient resume path is to stop debugging backend logic and go straight to a Playwright-wrapper or manual browser recording pass against the running local server.
</context>

<next_action>
Start with: use the Codex Playwright wrapper skill or an equivalent browser automation path against `http://localhost:3000`, paste the `GLP1R_HUMAN` FASTA, attach `output/playwright/glp1-demo/glp1-paper.pdf`, confirm the rendered report shows `GLP1R / P43220`, save a screenshot/video artifact, and then update the demo recording.
</next_action>
